# Secrets — never commit these
.env

# Runtime state (local fallback dedup cache — the primary cache now lives
# in a GitHub Gist, see README). Machine-specific either way, not source.
seen_articles.json

# Logs
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/